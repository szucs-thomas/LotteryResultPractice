import java.util.Random;import java.util.Scanner;public class Main {    public static void blockWritesOut(int[] block) {        for (int i = 0; i < block.length - 1; i++) {            System.out.print(block[i] + ", ");        }        System.out.println(block[block.length - 1]);    }    public static boolean contain(int[] block, int number) {        boolean include = false;        for (int element : block) {            if (element == number) {                include = true;                break;            }        }        return include;    }    public static int lotteryResult(int[] block1, int[] block2) {        int flag = 0;        for (int i = 0; i < block1.length; i++) {            for (int j = 0; j < block2.length; j++) {                if (block1[i] == block2[j]) {                    flag++;                }            }        }        if (flag == 0) {            System.out.println("I'm sorry, you didn't win. You don't have any matches.");        }        System.out.println();        System.out.println("Congratulations! You had " + flag + " matches in the lottery.");        return flag;    }    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        Random ran = new Random();        int[] winningNum = new int[7];        int[] tips = new int[7];        //Generate 7 different winning numbers.        for (int i = 0; i < winningNum.length; i++) {            int winnN = 0;            do {                winnN = ran.nextInt(35) + 1;            } while (contain(winningNum, winnN));            winningNum[i] = winnN;        }        //"I will show the winning numbers."        blockWritesOut(winningNum);        // Let's ask the user for 7 different guesses.        for (int i = 0; i < tips.length; i++) {            int tip_s = 0;            boolean again;            do {                System.out.print("Please enter your guess: ");                tip_s = sc.nextInt();                again = contain(tips, tip_s);                if (again && tip_s != 0) {                    System.out.println("I apologize for the repetition. Please enter another guess.");                } else if (tip_s < 1 || tip_s > 35) {                    System.out.println("Please give me guesses between 1 and 35.");                    again = true;                }            } while (again);            tips[i] = tip_s;        }        lotteryResult(winningNum, tips);    }}